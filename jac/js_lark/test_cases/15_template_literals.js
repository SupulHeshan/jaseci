// Test 15: Template literals
const simple = `Hello, World!`;
const withVar = `Value: ${x}`;
const multiline = `
    Line 1
    Line 2
    Line 3
`;

// Complex expressions
const expr = `Result: ${a + b * c}`;
const nested = `Outer ${`inner ${x}`}`;
const method = `Method: ${obj.method()}`;
